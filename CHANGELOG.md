## 0.0.8
* Added demo GIF to README.

## 0.0.7
* Added optional auto-play carousel (`autoPlay`, `autoPlayInterval`, `autoPlayAnimationDuration`).
* Auto-play pauses on user drag and resumes on release.
* Made `height` and `borderRadius` optional (nullable with defaults 220 / 12).

## 0.0.6
* fixing zoom

## 0.0.5

* Initial release with image slider, indicator, and full screen image preview.
